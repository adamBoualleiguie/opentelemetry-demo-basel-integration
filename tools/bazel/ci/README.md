# `tools/bazel/ci`

Scripts such as `ci_fast.sh`, `ci_full.sh`, and `affected_targets.sh` will be added in M4+.

M0: GitHub Actions runs Bazel directly in `.github/workflows/checks.yml` (`bazel_smoke` job).
